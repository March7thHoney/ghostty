# Loaded by the macOS app after the user's config so the bundled look always wins.
theme = light:Cyrene Light,dark:Cyrene Dark

# The app is an opaque desktop window; transparency would let each pane show a different backdrop.
background-opacity = 1
background-blur = false
